export interface PartOnePolicy {
    letter: string;
    minAmount: number;
    maxAmount: number;
}

export interface PartTwoPolicy {
    letter: string;
    firstPosition: number;
    secondPosition: number;
}